Do not add password strength meters, biometrics, payment flows, or new feature work yet.

The authentication system is now close to feature complete. Focus only on final production QA, cleanup, edge-case handling, and polish.

Final hardening pass requirements:

Performance + smoothness:

* Verify no dropped frames or animation stutter during auth transitions.
* Verify no unnecessary re-renders occur during typing or OTP entry.
* Verify keyboard animations remain smooth on both iOS and Android.
* Verify SVG rendering remains crisp during screen transitions and scaling.
* Verify all touch targets feel responsive with no delayed presses.

Visual QA:

* Verify spacing consistency across all auth screens.
* Verify Apple and Google buttons align perfectly with form widths and paddings.
* Verify typography hierarchy is visually consistent everywhere.
* Verify no clipped shadows, overflowing containers, or inconsistent corner radii exist.
* Verify loading states and disabled states visually match the design system exactly.
* Verify safe-area spacing on small and tall devices.

Auth flow QA:

* Stress-test rapid navigation between Sign In / Sign Up repeatedly.
* Stress-test repeated OTP resend attempts.
* Stress-test rapid tapping on CTAs during loading states.
* Verify auth flows recover gracefully after invalid input.
* Verify auth state resets correctly after sign out.
* Verify users cannot accidentally navigate into authenticated screens after sign out.
* Verify onboarding/auth screens cannot be back-swiped into after authentication.

Code cleanup:

* Remove any remaining placeholder/mock authentication logic.
* Remove any leftover temporary comments or debugging code.
* Remove any dead styles/components/routes.
* Ensure reusable auth utilities/hooks/components are properly organized.
* Ensure no duplicated validation or navigation logic remains.

Final validation requirements:

* Run full TypeScript checks again.
* Run Expo Go and verify zero warnings/errors.
* Verify all auth screens render correctly on both iOS and Android.
* Verify all routes and gestures behave production-ready.
* Confirm the authentication system is fully polished before beginning any post-auth product features.
