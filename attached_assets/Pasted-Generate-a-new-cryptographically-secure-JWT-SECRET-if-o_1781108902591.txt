Generate a new cryptographically secure JWT_SECRET if one does not already exist. Store it securely in the environment variables and update all backend authentication code to use it consistently for JWT signing and verification.

Then perform the remaining Google Sign-In verification and automatically fix all blocking issues.

Requirements:

1. Replace any web usage of EXPO_PUBLIC_API_URL=http://localhost:3000 with a same-origin HTTPS-compatible solution for Replit Web Preview.

2. Update backend CORS configuration to allow:

   * https://*.janeway.replit.dev
   * https://*.expo.janeway.replit.dev
   * localhost
   * Expo Go domains

3. Log and display:

   * Runtime redirect URI
   * window.location.origin
   * Generated AuthSession redirect URI
   * Full Google OAuth response

4. Verify Google.useAuthRequest initializes successfully.

5. Verify pressing the Google Sign-In button opens the Google OAuth popup.

6. Verify Google returns a valid access token.

7. Verify POST /api/auth/google receives the token.

8. Verify backend returns:

   * JWT
   * Authenticated user object

9. Verify JWT is stored successfully.

10. Verify session restoration after app restart.

11. Verify authenticated users are redirected to the PayVora home screen.

12. Continue diagnosing and automatically fixing issues until:

* The complete Google Sign-In flow passes successfully, OR
* A specific blocking error is identified with exact root cause.

13. If JWT_SECRET is missing:

* Generate a new secure 64+ character random secret.
* Add it to Replit Secrets.
* Update all server authentication middleware to use the new secret.
* Verify JWT creation and verification work correctly.

Provide a final report containing:

* Generated Runtime Redirect URI
* window.location.origin value
* AuthSession Redirect URI
* Current OAuth Status
* Exact Google Cloud OAuth Redirect URIs that must be configured
* JWT_SECRET Status (Generated / Existing)
* Files Modified
* Whether Google Sign-In works in Replit Web Preview
* Whether Google Sign-In works in Expo Go
* Any Remaining Blocking Issues
* Recommended Next Action
