{
  "build": {
    "cmds": [
      "pnpm --filter @workspace/api-server run build",
      "pnpm --filter @workspace/mobile exec expo export --platform web"
    ]
  },
  "deploy": {
    "startCmd": "node --enable-source-maps artifacts/api-server/dist/index.mjs"
  }
}
